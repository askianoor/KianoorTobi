/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CloseSidePanelDirective } from './close-side-panel.directive';

describe('Directive: CloseSidePanel', () => {
  it('should create an instance', () => {
    const directive = new CloseSidePanelDirective();
    expect(directive).toBeTruthy();
  });
});
