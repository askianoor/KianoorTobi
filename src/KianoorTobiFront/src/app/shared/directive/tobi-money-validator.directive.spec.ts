/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TobiMoneyValidatorDirective } from './tobi-money-validator.directive';

describe('Directive: TobiMoneyValidator', () => {
  it('should create an instance', () => {
    const directive = new TobiMoneyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
